/*
	C++ Strings
	They're part of the STL.
	#include <string>

	Some Methods & Properties: 
	(1)	string_name.at(position)
	(2) string_name.substr(start_index, length)
	(3) string_name.find(search_string)					RETURNS size_t
	(4) string_name.erase(start_index, length)
	(5) string_name.length()

	(6)	getline(cin, string_name)	,	cin >> 

	(7) string::npos	-->		value=-1, indicating no matches (find function)
*/

#include <iostream>
#include <string>
#include <locale>

using namespace std;

int main(void) {

	

	return 0;
}